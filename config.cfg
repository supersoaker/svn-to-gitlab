gitlab_host_url="https://git.example.com" 	# the base url of the gitlab server (without trailing slash)
gitlab_private_token="XXXXXXXXXXXXXXXXXXXX"	# your gitlab private token (get it from your profile settings (/profile/account))
namespace_name="XXX"				# the name space of the new repository
namespace_id="3"				# the id of the group or the user which is going to be the namespace of the repo

svn_host_url="https://svn.example.com"		# the old svn host url (just use the base url if you want to add the repository name dynamically)

